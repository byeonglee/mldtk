module list_expr_3;

mltype node = 
    Module of node list
  | ScopeDefinition of node
  ;

mltype raw_type = N of int;

mlvalue analyze n = n;

mlvalue foo = [1,2,3]
  ;

mlvalue bar = [true, false, true];

mlvalue baz = ["an", "ho", "ab", "cd"];

