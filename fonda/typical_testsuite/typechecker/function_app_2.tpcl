module function_app_2;

mltype node = 
    Module of node list
  |  ScopeDefinition of node
  |  M of string
  ;
mltype raw_type = N of int;
mlvalue analyze n = n ;

mlvalue foo (n:int) (m:int) = n + m;

mlvalue bar x y = let res = foo x y in res * 2;
